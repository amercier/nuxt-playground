{
  "private": "true",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "heroku-postbuild": "npm run build",
    "test": "ava",
    "dev-test": "ava --watch"
  },
  "dependencies": {
    "nuxt": "^1.4.1"
  },
  "engines": {
    "node": ">= 8",
    "npm": ">= 5"
  },
  "engineStrict": true,
  "devDependencies": {
    "ava": "^0.25.0",
    "jsdom": "^11.11.0"
  }
}
